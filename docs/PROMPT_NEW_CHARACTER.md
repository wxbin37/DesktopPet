# 新角色桌宠：可直接粘贴到新会话的 Prompt

复制下面代码块中的全部文字，在新会话中粘贴；同时上传新角色参考图。如果没有参考图，就把角色描述填写清楚。

```text
我不懂代码，也不会使用 GitHub，请你直接完成制作、测试和打包，不要只给我代码或命令。

请基于现有项目制作一个全新的独立桌宠角色，不要重写桌宠框架，也不要覆盖现有男生版和女生版：
https://github.com/wxbin37/DesktopPet

开始前请先获取最新 main 分支，并完整阅读：
- README.md
- docs/CHARACTER_TEMPLATE.md
- docs/DEVELOPMENT.md
- docs/WINDOWS.md
- docs/NEW_CHARACTER_PROJECT_TEMPLATE.md

新角色信息：
- 角色项目名称：[填写中文名]
- 程序英文名：[填写英文名，例如 DesktopPetCat]
- 角色类型：[男生/女生/动物/机器人/其他]
- 画风：[可爱动漫/Q版/像素/手绘/其他]
- 外形特征：[填写脸型、五官、发型或头部特征]
- 服装和配饰：[填写]
- 主色和气质：[填写]
- 参考图片：[我已在本会话上传/没有参考图]
- 需要保留的参考图特征：[填写]
- 功能要求：[功能完全沿用现有项目/填写需要修改的功能]
- 输出平台：[macOS 和 Windows]
- 是否保留自拍状态：[是/否]

执行要求：

1. 先检查仓库状态和电脑环境。创建一个完全独立的新角色文件夹或变体，不能影响现有两个角色。
2. 先分析参考图的脸型、五官、发型、服装、配饰、配色和气质，保持人物自己的特征，不要套用千篇一律的 Q 版模板。
3. 第一步只生成一张透明背景、完整全身、四周留透明边距的标准角色候选图。检查多头、多手、多腿、断肢、裁切、白边、阴影和透明背景后，暂停让我确认。未经确认不要生成其他动作，也不要接入程序。
4. 形象确认后，再生成待机、眨眼、自然走路、睡觉、醒来、开心、克制惊讶、拖拽、自拍和打字趴桌等状态。
5. 走路必须制作独立 4×2、共 8 帧透明 walking sprite sheet，包含接触、下压、经过、抬腿和换脚。必须有腿部交替、后腿向后蹬、身体轻微起伏、手臂反向摆动。禁止用整张图片缩放、压扁、左右抖动或闪烁来伪装走路。
6. 所有动作都要保持同一个人物；统一人物大小和脚底锚点，清理脚底阴影、倒影、圆弧、孤立碎片和相邻格子漏图，避免头发、手臂和鞋子被窗口 mask 裁切。
7. 生成走路 GIF 或预览后再次暂停，让我确认走路是否自然。未经确认不要最终打包。
8. 打字 UI 沿用现有 BongoCat/Mongocat 结构：小人趴在桌边，程序绘制真实键盘并高亮我实际按下的键位；只需要左手根据真实按键节奏下压和弹回，手掌、袖子和手臂必须连接自然。不能画两个键盘。
9. 键盘监听只能用于动画，不允许模拟按键、点击页面、记录或上传输入。桌宠必须始终置顶但不抢焦点，不能打断中文输入法候选框；可点击人物拖拽，透明区域不挡鼠标；自动移动只限拖放位置附近。
10. 如果保留自拍状态，展示的照片只能使用我提供的本地原图，不能用生成图代替；原图和私人素材不能提交 GitHub。
11. 人物和走路都确认后，再接入程序。执行素材生成、核心测试、导入测试、短启动测试和打包后启动测试；检查托盘菜单、右键菜单、退出和无残留进程。
12. 打包 macOS 可双击运行的 .app/.dmg，并通过现有 GitHub Actions 在真实 Windows x64 环境生成免安装 .exe 和分享用 .zip。核对 SHA-256。
13. 补充新角色说明文档，提交代码和公开素材到 GitHub；不要提交私人原图、.venv、build、dist 或临时包。
14. 完成后直接告诉我：macOS 打开哪个文件、Windows 打开哪个文件、分享给别人发送哪个文件，并给出本地绝对路径和 GitHub Release 地址。

除“角色候选图确认”和“走路预览确认”两个节点外，能由你执行的下载、命令、素材整理、代码修改、测试、提交、打包和下载请直接完成。只有确实需要我安装软件、授予系统权限或确认形象时，再用简单中文告诉我。
```
